import * as React from "react";

const HelloWorld = () => (
    <div>
        <h1>{"Hello, world!"}</h1>
        <hr />
        <small>{"{[{name}]}"}</small>
    </div>
);

export default HelloWorld;
